% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lump.R
\name{lump_nominal_supervised}
\alias{lump_nominal_supervised}
\title{Perform supervised lumping on a nominal variable}
\usage{
lump_nominal_supervised(
  data,
  outcome,
  threshold,
  adj_matrix = NULL,
  verbose = FALSE,
  level_namer = default_level_namer,
  outcome_mode = c("auto", "discrete", "continuous")
)
}
\arguments{
\item{data}{Factor or character vector of the categorical data.}

\item{outcome}{Factor or character vector. Variable to be used as a source of information about \code{data}.}

\item{threshold}{The minimum number of samples each lumped level should contain.}

\item{adj_matrix}{Adjancency matrix of the preference graph. Default: a complete graph, allowing all lumpings.}

\item{verbose}{Logical value dictating if values should be printed. Default: \code{FALSE}.}

\item{level_namer}{Function that takes a character vector of the original levels in a lump and returns the name of
the new lumped level. Default: concatenating the original levels with a "+" in between.}

\item{outcome_mode}{Whether to treat the outcome as discrete or continuous. Default: inferred based on the type of \code{outcome}.}
}
\value{
A factor vector with the lumped levels.
}
\description{
Lumps the levels of a nominal variable so as to preserve as much mutual
information as possible between the lumped variable and a supplied \code{outcome}.
The outcome may be discrete (a factor) or continuous (numeric); see
\code{vignette("supervised")} for the distinction.
}
\examples{
data    <-        c("NL", "NL", "DE", "DE", "FR", "FR", "FR", "BE")
outcome <- factor(c(  1,    0,    1,    1,    0,    0,    1,    1))
lump_nominal_supervised(data, outcome, threshold = 3)

}
\seealso{
\code{\link[=maximum_mutual_information_nominal_supervised]{maximum_mutual_information_nominal_supervised()}} for the underlying algorithm that this function wraps.

\code{\link[=lump_hierarchical_supervised]{lump_hierarchical_supervised()}} for a version of this function that can take advantage of hierarchical structure in the data to speed up the execution time.
}
\author{
Daan Koning
}
