% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/supervised.R
\name{maximum_mutual_information_nominal_supervised}
\alias{maximum_mutual_information_nominal_supervised}
\title{Maximum information preservable by supervised nominal lumping}
\usage{
maximum_mutual_information_nominal_supervised(
  joint_counts,
  threshold,
  adj_matrix = NULL,
  verbose = FALSE
)
}
\arguments{
\item{joint_counts}{Named numeric matrix with one row per level and one
column per outcome category. Row names must identify the levels.
Entry (k, y) is the number of observations with covariate level k and
outcome y.}

\item{threshold}{Minimum number of samples each lumped level must contain.}

\item{adj_matrix}{Adjacency matrix of the preference graph. Default: a
complete graph, allowing all lumpings.}

\item{verbose}{Whether to print diagnostic messages. Default: \code{FALSE}.}
}
\value{
A list containing information about the optimal lumping:
\describe{
\item{mutual_information}{Mutual information between the lumped covariate
and the outcome, in nats.}
\item{loss}{Mutual information lost by the lumping.}
\item{lumping}{A list of character vectors, where each vector contains the
names of the original levels that have been lumped together.}
}
}
\description{
Calculates the maximum amount of mutual information between a lumped nominal
covariate and a discrete outcome that can be preserved by lumping.
}
\details{
Be advised that, since the problem is NP-hard, the implementation here has
time complexity \eqn{O\left(2^{2^m}\right)}{O(2^2^m)}, where \eqn{m}{m} is
the number of levels in the nominal variable.
}
\examples{
joint_counts <- matrix(
  c(8, 2, 1, 4, 1, 2),
  nrow = 3,
  dimnames = list(c("A", "B", "C"), c("y0", "y1"))
)
maximum_mutual_information_nominal_supervised(joint_counts, threshold = 3)

}
\seealso{
\code{\link[=maximum_mutual_information_nominal]{maximum_mutual_information_nominal()}} for the unsupervised version.

\code{\link[=maximum_mutual_information_hierarchical_supervised]{maximum_mutual_information_hierarchical_supervised()}} for a version that
exploits hierarchical structure to speed up execution.

\code{\link[=maximum_mutual_information_nominal_supervised_continuous]{maximum_mutual_information_nominal_supervised_continuous()}} for a version that accepts a continuous outcome.
}
\author{
Daan Koning
}
