% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{adjacency_from_edge_list}
\alias{adjacency_from_edge_list}
\title{Transform the edge list representation of a graph into an adjacency matrix}
\usage{
adjacency_from_edge_list(levels, allow = NULL, disallow = NULL)
}
\arguments{
\item{levels}{The levels (and hence nodes) of the graph. This is needed since not all nodes neccesarily have an edge.}

\item{allow, disallow}{List of vectors of length 2 representing the edges that should be included or excluded from the
graph. Exactly one of \code{allow} or \code{disallow} must be passed.}
}
\value{
The adjacency matrix.
}
\description{
This helper is useful for generating the inputs for functions
that expect preference graphs.
}
\examples{
levels <- c("A", "B", "C", "D")
edges <- list(c("A", "C"), c("D", "B"))
# Include the pairings from `edges`:
adjacency_from_edge_list(levels, allow = edges)
# Or exclude them:
adjacency_from_edge_list(levels, disallow = edges)

}
\author{
Daan Koning
}
