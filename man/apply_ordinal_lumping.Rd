% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{apply_ordinal_lumping}
\alias{apply_ordinal_lumping}
\title{Apply a lumping to an ordinal variable}
\usage{
apply_ordinal_lumping(data, lumping, level_namer = default_level_namer)
}
\arguments{
\item{data}{Factor or character vector of the categorical data. If \code{data} is an ordered factor, the
lumping must respect its level order.}

\item{lumping}{Named list of character vectors, where each vector contains the original levels that are
combined into a new level named after the entry. If the list is unnamed, names are generated with \code{level_namer}.}

\item{level_namer}{Function that takes a character vector of the original levels in a lump and returns the name of
the new lumped level. Only used when \code{lumping} is unnamed. Default: concatenating the original levels with a "+" in between.}
}
\value{
An ordered factor vector with the lumped levels.
}
\description{
Transforms ordered categorical data by combining its levels according to a
given lumping. The order of the entries of the lumping defines the order of
the new levels, from lowest to highest. This makes it possible to reuse a
lumping, for example one learned on training data, on new data.
}
\details{
Levels that appear in the lumping but not in the data are kept as empty
levels, so that applying the same lumping always yields the same factor
levels. Conversely, levels that appear in the data but not in the lumping
are replaced by \code{NA}, with a warning.
}
\examples{
risk_group <- c("low", "high", "medium", "low", "very high")
lumping <- list("low" = "low", "medium+" = c("medium", "high", "very high"))
apply_ordinal_lumping(risk_group, lumping)

}
\seealso{
\code{\link[=lump_ordinal]{lump_ordinal()}} to find and apply the optimal lumping in one step.

\code{\link[=apply_nominal_lumping]{apply_nominal_lumping()}} for the nominal analogue of this function.
}
\author{
Daan Koning
}
