% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lump.R
\name{lump_hierarchical}
\alias{lump_hierarchical}
\title{Perform lumping on a hierarchical nominal variable}
\usage{
lump_hierarchical(
  data,
  threshold,
  clusters,
  verbose = FALSE,
  alternative_metric = c("mutual information", "bin count", "surplus"),
  level_namer = default_level_namer
)
}
\arguments{
\item{data}{Factor or character vector of the categorical data.}

\item{threshold}{The minimum number of samples each lumped level should contain.}

\item{clusters}{List of character vectors representing the levels that are allowed to be lumped together.}

\item{verbose}{Logical value dictating if values should be printed. Default: \code{FALSE}.}

\item{alternative_metric}{The metric that should be optimised for, if it is different from the default,
the mutual information. For an explanation of the metrics see \code{vignette("metrics")}.}

\item{level_namer}{Function that takes a character vector of the original levels in a lump and returns the name of
the new lumped level. Default: concatenating the original levels with a "+" in between.}
}
\value{
A factor vector with the lumped levels.
}
\description{
Lumps the levels of a nominal variable that carries a known hierarchy (for
example countries grouped into continents). Only levels within the same
cluster are combined, which preserves interpretability and lets the algorithm
run substantially faster than the fully general \code{\link[=lump_nominal]{lump_nominal()}}.
}
\examples{
country <- c("Germany", "Netherlands", "France", "France", "China", "China",
             "China", "China", "Vietnam", "Vietnam", "Japan", "Japan")
lump_hierarchical(
     country,
     4,
     list(c("Germany", "Netherlands", "France"), c("China", "Vietnam", "Japan"))
)

}
\seealso{
\code{\link[=maximum_mutual_information_hierarchical]{maximum_mutual_information_hierarchical()}} for the underlying algorithm that this function wraps.

\code{\link[=lump_nominal]{lump_nominal()}} for a more general version of this function that does not need the hierarchical structure in the data, but may be slower.

\code{\link[=lump_hierarchical_supervised]{lump_hierarchical_supervised()}} for a supervised version of this function.
}
\author{
Daan Koning
}
