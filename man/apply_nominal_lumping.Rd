% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{apply_nominal_lumping}
\alias{apply_nominal_lumping}
\title{Apply a lumping to a nominal variable}
\usage{
apply_nominal_lumping(data, lumping, level_namer = default_level_namer)
}
\arguments{
\item{data}{Factor or character vector of the categorical data.}

\item{lumping}{Named list of character vectors, where each vector contains the original levels that are
combined into a new level named after the entry. If the list is unnamed, names are generated with \code{level_namer}.}

\item{level_namer}{Function that takes a character vector of the original levels in a lump and returns the name of
the new lumped level. Only used when \code{lumping} is unnamed. Default: concatenating the original levels with a "+" in between.}
}
\value{
A factor vector with the lumped levels.
}
\description{
Transforms categorical data by combining its levels according to a given
lumping, such as one previously found by \code{\link[=maximum_mutual_information_nominal]{maximum_mutual_information_nominal()}}.
This makes it possible to reuse a lumping, for example one learned on
training data, on new data.
}
\details{
Levels that appear in the lumping but not in the data are kept as empty
levels, so that applying the same lumping always yields the same factor
levels. Conversely, levels that appear in the data but not in the lumping
are replaced by \code{NA}, with a warning.
}
\examples{
country <- c("NL", "DE", "FR", "NL", "BE")
lumping <- list(benelux = c("NL", "BE"), other = c("DE", "FR"))
apply_nominal_lumping(country, lumping)

}
\seealso{
\code{\link[=lump_nominal]{lump_nominal()}} to find and apply the optimal lumping in one step.

\code{\link[=apply_ordinal_lumping]{apply_ordinal_lumping()}} for the ordinal analogue of this function.
}
\author{
Daan Koning
}
