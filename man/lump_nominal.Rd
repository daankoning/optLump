% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lump.R
\name{lump_nominal}
\alias{lump_nominal}
\title{Perform lumping on a nominal variable}
\usage{
lump_nominal(
  data,
  threshold,
  adj_matrix = NULL,
  verbose = FALSE,
  alternative_metric = c("mutual information", "bin count", "surplus"),
  level_namer = default_level_namer
)
}
\arguments{
\item{data}{Factor or character vector of the categorical data.}

\item{threshold}{The minimum number of samples each lumped level should contain.}

\item{adj_matrix}{Adjancency matrix of the preference graph. Default: a complete graph, allowing all lumpings.}

\item{verbose}{Logical value dictating if values should be printed. Default: \code{FALSE}.}

\item{alternative_metric}{The metric that should be optimised for, if it is different from the default,
the mutual information. For an explanation of the metrics see \code{vignette("metrics")}.}

\item{level_namer}{Function that takes a character vector of the original levels in a lump and returns the name of
the new lumped level. Default: concatenating the original levels with a "+" in between.}
}
\value{
A factor vector with the lumped levels.
}
\description{
Lumps the levels of an unordered categorical variable so that every resulting
level meets the sample-size \code{threshold}, choosing the combination that
preserves the most mutual information with the original variable. An optional
preference graph restricts which levels may be combined.
}
\examples{
m <- 5
n <- 50
q <- 10
data <- sample(LETTERS[1:m], n, replace = TRUE)
lump_nominal(data, q)

}
\seealso{
\code{\link[=maximum_mutual_information_nominal]{maximum_mutual_information_nominal()}} for the underlying algorithm that this function wraps.

\code{\link[=lump_hierarchical]{lump_hierarchical()}} for a version of this function that can take advantage of hierarchical structure in the data to speed up the execution time.

\code{\link[=lump_nominal_heuristic]{lump_nominal_heuristic()}} to approximate this function when the runtime becomes infeasible.

\code{\link[=lump_nominal_supervised]{lump_nominal_supervised()}} for a supervised version of this function.
}
\author{
Daan Koning
}
