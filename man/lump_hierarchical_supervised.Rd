% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lump.R
\name{lump_hierarchical_supervised}
\alias{lump_hierarchical_supervised}
\title{Perform supervised lumping on a hierarchical nominal variable}
\usage{
lump_hierarchical_supervised(
  data,
  outcome,
  threshold,
  clusters,
  verbose = FALSE,
  level_namer = default_level_namer
)
}
\arguments{
\item{data}{Factor or character vector of the categorical data.}

\item{outcome}{Factor or character vector. Variable to be used as a source of information about \code{data}.}

\item{threshold}{The minimum number of samples each lumped level should contain.}

\item{clusters}{List of character vectors representing the levels that are allowed to be lumped together.}

\item{verbose}{Logical value dictating if values should be printed. Default: \code{FALSE}.}

\item{level_namer}{Function that takes a character vector of the original levels in a lump and returns the name of
the new lumped level. Default: concatenating the original levels with a "+" in between.}
}
\value{
A factor vector with the lumped levels.
}
\description{
Lumps a hierarchical nominal variable (combining only levels within the same
cluster) so as to preserve as much mutual information as possible with a
supplied \code{outcome}.
}
\details{
Note that, unlike \code{\link[=lump_nominal_supervised]{lump_nominal_supervised()}} and \code{\link[=lump_ordinal_supervised]{lump_ordinal_supervised()}}, this function only supports
discrete (factor) outcomes, not continuous ones.
}
\examples{
data <- c("Utrecht", "Utrecht", "Friesland",
          "Friesland", "Friesland", "Bayern",
          "Bayern", "Bayern", "Sachsen")
outcome <- c(1, 0, 1, 1, 0, 0, 0, 1, 1)
clusters <- list(
  c("Utrecht", "Friesland"),
  c("Bayern", "Sachsen")
)
lump_hierarchical_supervised(data, outcome, threshold = 3, clusters = clusters)

}
\seealso{
\code{\link[=maximum_mutual_information_hierarchical_supervised]{maximum_mutual_information_hierarchical_supervised()}} for the underlying algorithm that this function wraps.

\code{\link[=lump_nominal_supervised]{lump_nominal_supervised()}} for a more general version of this function that does not need the hierarchical structure in the data, but may be slower.
}
\author{
Daan Koning
}
